# --- CLIENT CONFIGURATION ---

# The base URL for the backend API.
# Note: In development using Vite, the proxy in vite.config.js often handles this, 
# but this variable is used in slices/apiSlice.js to define the base query.
VITE_API_URL=http://localhost:5000/api
